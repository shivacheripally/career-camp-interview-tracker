# Career Camp Interview Tracker

This repository contains the server-side implementation for the Career Camp Interview Tracker project.

## Tech Stack

- EJS
- Express
- MongoDB / Mongoose
- Passport
- CSS
- JavaScript
- Backend

## Code-Based Notes

- MongoDB persistence is configured through Mongoose models.
- Authentication middleware is handled with Passport.
- Route modules present in the repository include: employees, index, interviews, students.

## Project Structure

- `/.gitignore`
- `/assets`
- `/config`
- `/controllers`
- `/index.js`
- `/models`
- `/package.json`
- `/package-lock.json`

## Local Setup

1. Install dependencies with `npm install`.
2. Start the project with `npm start`.

## Available Scripts

- `npm run start` or `npm start` as defined in `package.json`
- `npm run test` or `npm test` as defined in `package.json`

## Status

This README was refreshed from the current repository structure and source files. Update it further when deployment details, screenshots, or environment variable examples are available.

